<% @categories.each do |category| %>
    <li class="nav-item" style="margin: 0px 0px 0px -10px;">
      <% category.name %>
      <%= link_to category.name, category, class: 'btn btn-outline-success my-2 sm-0' %>
    </li>
<% end %>